name: Claim flow CI

on:
  push:
    branches:
      - fix/institute-claim-token-flow
  pull_request:
    paths:
      - "index.js"
      - "src/services/**"
      - "__tests__/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig*.json"
      - ".github/workflows/claim-flow-ci.yml"

permissions:
  contents: read

jobs:
  student-app-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    env:
      EXPO_PUBLIC_API_BASE_URL: https://backend.kormic.ai/api
      CI: "true"

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Strict type-check for claim transport
        run: npx tsc --noEmit -p tsconfig.claim-flow.json

      - name: Lint complete application
        run: npm run lint

      - name: Run complete Jest suite
        run: npm test

      # The main branch already has two unrelated ProfileScreen diagnostics.
      # Keep reporting the full-project result without allowing those existing
      # issues to hide regressions in this isolated claim-flow repair.
      - name: Report full-project type-check baseline
        continue-on-error: true
        run: npm run typecheck
